"""
Типы операций
"""
type OperationType {
    id: ID
    name: String
}

extend type Query  @guard{
    """
    Получить список типов операций
    """
   operationTypes(where: _ @whereConditions(columns: ["fee_type_id"])): [OperationType!]!   @all
}
