"""
Типы операций
"""
type OperationType {
    id: ID
    name: String
}

extend type Query  @guard{
    """
    Получить список типов операций
    """
   operationTypes: [OperationType!]! @can(ability: "view", query: true) @all
}
