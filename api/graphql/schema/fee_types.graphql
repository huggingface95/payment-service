"""
Типы платежей
"""
type FeeType {
    id: ID!
    "Название"
    name: String!
}

extend type Query @guard {
    """
    Получить список статусов
    """
   feeTypes: [FeeType!]! @can(ability: "view") @all
}
