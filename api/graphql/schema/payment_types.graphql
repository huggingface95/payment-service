"""
Типы платежей
"""
type PaymentTypes {
    id: ID!
    "Название"
    name: String!
}

extend type Query @guard {
    """
    Получить список
    """
   getPaymentTypes: [PaymentTypes!]! @can(ability: "view") @all
}
