"""
Типы платежей
"""
type PaymentStatus {
    id: ID!
    "Название"
    name: String!
}

extend type Query  @guard{
    """
    Получить список статусов
    """
   paymentStatuses: [PaymentStatus!]! @can(ability: "view", query: true) @all
}
